const cuenta = -10;

const mensaje = 
    
//cuenta <0 ? "no tienes saldo " : "tienes saldo ";   //ESTE TIENE UNA CONDICION PARA EL SI Y EL NO
    cuenta < 0 && "hola"; // ESTE TIENE SOLO CONDICION PARA EL SI, SI ES NO VA A MOSTRAR "FALSE"





document.write(mensaje);